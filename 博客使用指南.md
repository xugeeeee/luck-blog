# 博客使用指南

> 🌐 博客地址：<https://iridescent-flan-30c407.netlify.app/>

本指南详细介绍如何管理和更新你的 Hexo 博客。

---

## 目录

1. [撰写文章与更新博客](#一撰写文章与更新博客)
2. [博客功能编辑与配置](#二博客功能编辑与配置)
3. [Git 推送操作](#三git-推送操作)
4. [部署相关操作](#四部署相关操作)

---

## 一、撰写文章与更新博客

### 1.1 创建新文章

在博客目录下运行以下命令：

```bash
# 进入博客目录
cd c:\Users\wangh\Desktop\workspace\lqbz\工作台\my-blog

# 创建新文章（将"文章标题"替换为你的实际标题）
npx hexo new "文章标题"
```

这会在 `source/_posts/` 目录下创建一个新的 Markdown 文件，例如：`source/_posts/文章标题.md`

### 1.2 文章格式

每篇文章包含 **Front-matter（头部信息）** 和 **正文内容**：

```markdown
---
title: 文章标题
date: 2026-01-12 10:00:00
tags:
  - 标签1
  - 标签2
categories:
  - 分类名
---

这里是文章正文内容...

## 二级标题

### 三级标题

正文支持完整的 Markdown 语法。
```

#### Front-matter 常用字段说明

| 字段 | 说明 | 示例 |
|------|------|------|
| `title` | 文章标题 | `title: 我的第一篇博客` |
| `date` | 发布时间 | `date: 2026-01-12 10:00:00` |
| `tags` | 标签（可多个） | `tags: [Hexo, 教程]` |
| `categories` | 分类 | `categories: 技术` |
| `top_img` | 文章顶部图片 | `top_img: /img/banner.jpg` |
| `cover` | 文章封面图 | `cover: /img/cover.jpg` |
| `description` | 文章描述 | `description: 简短描述` |

### 1.3 本地预览

写完文章后，启动本地服务器预览效果：

```bash
# 启动本地服务器
npx hexo server

# 或使用 npm 脚本
npm run server
```

访问 `http://localhost:4000` 查看效果。

### 1.4 完整发布流程

```bash
# 1. 清除缓存（可选，推荐）
npx hexo clean

# 2. 生成静态文件
npx hexo generate
# 或
npm run build

# 3. 提交到 Git 并推送（会自动触发 Netlify 部署）
git add .
git commit -m "发布新文章：文章标题"
git push
```

---

## 二、博客功能编辑与配置

### 2.1 更新"关于"页面

**文件位置**：`source/about/index.md`

直接编辑该文件即可更新关于页面内容：

```markdown
---
title: about
date: 2026-01-12 09:38:33
---

## 关于我

你好！我是 wangh。

### 个人简介

在这里写你的个人介绍...

### 技能

- 技能1
- 技能2

### 联系方式

- 邮箱：xxx@example.com
- GitHub：github.com/xxx
```

### 2.2 站点基本信息配置

**文件位置**：`_config.yml`（根目录）

```yaml
# 站点信息
title: 我的个人博客           # 博客标题
subtitle: '记录成长，分享知识'  # 副标题
description: '一个记录学习、生活和思考的个人博客'  # 描述
keywords: 博客, 技术, 生活     # 关键词
author: wangh                  # 作者名
language: zh-CN                # 语言
timezone: 'Asia/Shanghai'      # 时区

# URL 配置
url: https://iridescent-flan-30c407.netlify.app  # 改为你的实际网址
```

### 2.3 主题配置（Butterfly）

**文件位置**：`_config.butterfly.yml`（根目录）

#### 导航菜单配置

```yaml
menu:
  首页: / || fas fa-home
  归档: /archives/ || fas fa-archive
  标签: /tags/ || fas fa-tags
  分类: /categories/ || fas fa-folder-open
  关于: /about/ || fas fa-heart
  # 添加新菜单示例：
  # 友链: /link/ || fas fa-link
```

#### 头像与社交链接

```yaml
# 头像设置
avatar:
  img: /img/butterfly-icon.png  # 替换为你的头像路径
  effect: false                  # 是否启动旋转效果

# 社交媒体链接
social:
  fab fa-github: https://github.com/xxxxx || Github || '#24292e'
  fas fa-envelope: mailto:xxxxxx@gmail.com || Email || '#4a7dbe'
```

#### 侧边栏作者卡片

```yaml
aside:
  card_author:
    enable: true
    description: "一句话简介"  # 侧边栏作者描述
    button:
      enable: true
      icon: fab fa-github
      text: Follow Me
      link: https://github.com/xxxxxx  # 你的 GitHub 链接

  card_announcement:
    enable: true
    content: 欢迎来到我的博客！  # 公告内容
```

### 2.4 创建标签/分类/友链页面

```bash
# 创建标签页面
npx hexo new page tags

# 创建分类页面
npx hexo new page categories

# 创建友链页面
npx hexo new page link
```

创建后编辑对应的 `source/xxx/index.md` 文件，添加相应的 `type` 字段：

**标签页（source/tags/index.md）**：

```markdown
---
title: 标签
date: 2026-01-12 10:00:00
type: "tags"
---
```

**分类页（source/categories/index.md）**：

```markdown
---
title: 分类
date: 2026-01-12 10:00:00
type: "categories"
---
```

---

## 三、Git 推送操作

### 3.1 基本推送流程

```bash
# 1. 查看文件变更状态
git status

# 2. 添加所有变更到暂存区
git add .

# 3. 提交变更（写清晰的提交信息）
git commit -m "你的提交说明"

# 4. 推送到远程仓库
git push
# 或指定分支
git push origin main
```

### 3.2 常见提交信息模板

| 场景 | 提交信息示例 |
|------|-------------|
| 发布新文章 | `git commit -m "发布文章：Linux驱动开发入门"` |
| 更新文章 | `git commit -m "更新文章：修复代码示例错误"` |
| 更新关于页 | `git commit -m "更新关于页面：添加新技能"` |
| 修改配置 | `git commit -m "配置：更新站点标题和描述"` |
| 主题调整 | `git commit -m "主题：调整导航菜单"` |

### 3.3 常用 Git 命令

```bash
# 查看提交历史
git log --oneline -10

# 查看文件差异
git diff

# 撤销未暂存的修改
git checkout -- <文件名>

# 撤销暂存区的文件
git reset HEAD <文件名>

# 强制推送（谨慎使用）
git push -f

# 拉取远程更新
git pull
```

### 3.4 处理冲突

如果推送时遇到冲突：

```bash
# 1. 先拉取远程变更
git pull

# 2. 解决冲突（编辑冲突文件）
# 冲突标记：<<<<<<< HEAD ... ======= ... >>>>>>>

# 3. 重新提交
git add .
git commit -m "解决合并冲突"
git push
```

---

## 四、部署相关操作

### 4.1 部署原理

你的博客使用 **Netlify** 进行自动部署：

1. 当你 `git push` 代码到 GitHub 仓库时
2. Netlify 会自动检测到代码变更
3. Netlify 自动执行 `npm run build`（即 `hexo generate`）
4. 构建完成后自动部署到 CDN

### 4.2 Netlify 配置说明

**文件位置**：`netlify.toml`

```toml
[build]
  publish = "public"           # 发布目录
  command = "npm run build"    # 构建命令

[build.environment]
  NODE_VERSION = "20"          # Node.js 版本
```

### 4.3 完整部署流程

```bash
# 1. 确保本地构建成功
npx hexo clean
npx hexo generate

# 2. 本地预览确认无误
npx hexo server
# 访问 http://localhost:4000 检查

# 3. 提交并推送代码
git add .
git commit -m "更新内容"
git push

# 4. 等待 Netlify 自动部署（通常 1-3 分钟）
# 部署完成后访问：https://iridescent-flan-30c407.netlify.app/
```

### 4.4 在 Netlify 控制台查看部署状态

1. 访问 [Netlify 控制台](https://app.netlify.com/)
2. 选择你的站点
3. 查看 **Deploys** 页面了解部署历史和状态
4. 点击具体的部署可以查看构建日志

### 4.5 手动触发重新部署

如果需要手动触发部署：

1. 登录 Netlify 控制台
2. 进入站点设置
3. 点击 **Deploys** → **Trigger deploy** → **Deploy site**

### 4.6 自定义域名（可选）

如果需要绑定自定义域名：

1. 在 Netlify 控制台进入 **Site settings** → **Domain management**
2. 点击 **Add custom domain**
3. 输入你的域名（如 `blog.example.com`）
4. 按照指引配置 DNS 记录

---

## 五、常见问题

### Q1: 文章没有显示在首页？

- 检查文章的 `date` 是否为未来时间
- 运行 `npx hexo clean` 后重新生成

### Q2: 标签/分类页面空白？

- 确保创建了对应页面并设置了正确的 `type` 字段
- 确保文章中正确使用了 `tags` 和 `categories`

### Q3: 图片无法显示？

- 将图片放在 `source/img/` 目录下
- 使用相对路径引用：`![描述](/img/图片名.jpg)`

### Q4: 部署失败？

- 检查 Netlify 构建日志查找错误原因
- 确保本地 `npx hexo generate` 可以成功执行
- 检查 `package.json` 中的依赖是否完整

---

## 六、常用命令速查

| 命令 | 说明 |
|------|------|
| `npx hexo new "标题"` | 创建新文章 |
| `npx hexo new page "页面"` | 创建新页面 |
| `npx hexo generate` / `npm run build` | 生成静态文件 |
| `npx hexo server` / `npm run server` | 启动本地服务器 |
| `npx hexo clean` / `npm run clean` | 清除缓存 |
| `git add .` | 暂存所有更改 |
| `git commit -m "说明"` | 提交更改 |
| `git push` | 推送到远程仓库 |

---

## 七、目录结构参考

```
my-blog/
├── _config.yml              # 站点主配置文件
├── _config.butterfly.yml    # Butterfly 主题配置文件
├── netlify.toml             # Netlify 部署配置
├── package.json             # 项目依赖配置
├── source/
│   ├── _posts/              # 文章目录 ⭐
│   │   └── hello-world.md   # 示例文章
│   ├── about/               # 关于页面
│   │   └── index.md
│   ├── categories/          # 分类页面
│   └── tags/                # 标签页面
├── themes/                  # 主题文件
└── public/                  # 生成的静态文件（自动生成）
```

---

**祝你博客写作愉快！** 🎉
